// 9. Loop for + condicional (Ímpares de 1 a 15)

// O "% 2" pega o resto da divisão por 2. 
    // Se o resto for diferente de zero (!== 0), sabemos que o número é ímpar.

for (let i = 1; i <= 15; i++) {
    if (i % 2 !== 0) 
    console.log(i); }

